import { GridContainer } from './grid-container';

describe('GridContainer', () => {
  it('should create an instance', () => {
    const directive = new GridContainer();
    expect(directive).toBeTruthy();
  });
});
