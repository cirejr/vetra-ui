import { AvatarImage } from './avatar-image';

describe('AvatarImage', () => {
  it('should create an instance', () => {
    const directive = new AvatarImage();
    expect(directive).toBeTruthy();
  });
});
