import { AlertDialog } from './alert-dialog';

describe('AlertDialog', () => {
  it('should create an instance', () => {
    const directive = new AlertDialog();
    expect(directive).toBeTruthy();
  });
});
